apiVersion: v1
kind: Service
metadata:
  name: minecraft-svc
  namespace: minecraft
spec:
  type: NodePort
  ports:
  - port: 25565
    targetPort: 25565
    nodePort: 30005
  selector:
    app: minecraft
